{
  "name": "beckn-schema-validator-experiment",
  "version": "1.0.0",
  "description": "Experiment: additionalProperties vs unevaluatedProperties in JSON Schema composition",
  "scripts": {
    "test": "node --test",
    "validate": "node src/validator.js"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
